{
  "name": "snapfile-client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:crypto": "node --test tests/*.test.js",
    "dev": "vite --config vite.config.js",
    "build": "vite build --config vite.config.js",
    "preview": "vite preview --config vite.config.js",
    "test:e2e": "SNAPFILE_E2E=1 npm run build && playwright test",
    "test:e2e:ui": "SNAPFILE_E2E=1 npm run build && playwright test --ui",
    "test:e2e:headed": "SNAPFILE_E2E=1 npm run build && playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "dependencies": {
    "libsodium-wrappers": "0.8.4",
    "qrcode": "^1.5.3",
    "vue": "^3.5.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-vue": "^5.1.2",
    "vite": "^5.4.1"
  }
}
